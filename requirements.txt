# ── Core agent framework ────────────────────────────────
langgraph>=0.2.0
langchain>=0.1.0
langchain-core>=0.1.0

# ── LLM client ──────────────────────────────────────────
groq>=0.8.0

# ── Schema validation ────────────────────────────────────
pydantic>=2.0.0

# ── Environment ──────────────────────────────────────────
python-dotenv>=1.0.0

# ── Vector memory (optional — falls back to mock) ───────
chromadb>=0.4.0

# ── Image generation (Phase 1) ───────────────────────────
Pillow>=10.0.0
# Stable Diffusion from Hugging Face (primary backend):
diffusers>=0.25.0
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.25.0

# ── Phase 2: Audio synthesis ──────────────────────────────
gTTS>=2.5.0
pydub>=0.25.1
soundfile>=0.12.1
numpy>=1.24.0
pyttsx3>=2.90          # Windows TTS
# elevenlabs             # ElevenLabs API (set ELEVENLABS_ENABLED=1)

# ── Phase 2 / 3: Video + face processing ─────────────────
opencv-python>=4.8.0
moviepy                # video compositing
ffmpeg-python          # Python bindings for FFmpeg

# ── Phase 4: Web interface ────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# ── Testing ───────────────────────────────────────────────
pytest>=7.0.0
pytest-asyncio>=0.21.0
